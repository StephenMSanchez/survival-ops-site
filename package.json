{
  "name": "survival-ops-site",
  "version": "1.0.0",
  "private": true,
  "description": "Passcode-gated static site for survival reference info and team operations, with per-page, admin, and rotating TOTP access codes.",
  "scripts": {
    "build": "node build.js",
    "serve": "npx --yes serve dist -l 8080"
  },
  "dependencies": {
    "qrcode": "^1.5.3"
  }
}
